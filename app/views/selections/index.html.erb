<h1>Your Selections</h1>

<%= render 'list' %>



<!--   <table>
    <tr>
      <th>Name</th>
      <th>Public URL</th>
      <th></th>
      <th></th>
    </tr>
  <% @selections.each do |selection| %>
    <tr>
      <td><%= link_to selection.name, selection %></td>
      <td><%= link_to 'needsustenance.com/selections/'+selection.name, selection %></td>
      <td><%= link_to 'Edit', edit_selection_path(selection) %></td>
      <td><%= link_to 'Destroy', selection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </table>

<br />
 -->
<%= link_to 'New Selection', new_selection_path, class: "btn" %>

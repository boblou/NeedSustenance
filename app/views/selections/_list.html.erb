<% @selections.each do |selection| %>
    <h3><%= selection.name %></h3>
    <h4><%= link_to 'needsustenance.heroku.com/selections/'+selection.name, selection %><h4>
        <%= link_to 'Edit', edit_selection_path(selection), class: "btn" %>
        <%= link_to 'Delete', selection, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn" %>
    <br />
    <br />


  <% end %>

<br />